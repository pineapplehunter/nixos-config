---
name: pueue
description: Use pueue to run, inspect, wait for, and read logs of background tasks via a local pueued daemon.
---

# Pueue
Assume local `pueued` is running. Use `pueue add -- <cmd>` for tasks, `pueue status` to inspect, `pueue wait [ids...]` to block until done, and `pueue log [ids...]` for output. More subcommands/options: `pueue --help`.
