diff --git a/packages/opencode/src/cli/cmd/tui/component/spinner.tsx b/packages/opencode/src/cli/cmd/tui/component/spinner.tsx
index 700780314..462b60fb0 100644
--- a/packages/opencode/src/cli/cmd/tui/component/spinner.tsx
+++ b/packages/opencode/src/cli/cmd/tui/component/spinner.tsx
@@ -7,14 +7,27 @@ import "opentui-spinner/solid"
 
 export const SPINNER_FRAMES = ["⠋", "⠙", "⠹", "⠸", "⠼", "⠴", "⠦", "⠧", "⠇", "⠏"]
 
-export function Spinner(props: { children?: JSX.Element; color?: RGBA }) {
+function formatElapsed(seconds: number): string {
+  const h = Math.floor(seconds / 3600)
+  const m = Math.floor((seconds % 3600) / 60)
+  const s = seconds % 60
+  if (h > 0) return `${h}h ${m}m ${s}s`
+  if (m > 0) return `${m}m ${s}s`
+  return `${s}s`
+}
+
+export function Spinner(props: { children?: JSX.Element; color?: RGBA; elapsed?: number }) {
   const { theme } = useTheme()
   const kv = useKV()
   const color = () => props.color ?? theme.textMuted
+  const showElapsed = () => props.elapsed !== undefined && props.elapsed >= 10
   return (
     <Show when={kv.get("animations_enabled", true)} fallback={<text fg={color()}>⋯ {props.children}</text>}>
       <box flexDirection="row" gap={1}>
         <spinner frames={SPINNER_FRAMES} interval={80} color={color()} />
+        <Show when={showElapsed()}>
+          <text fg={color()}>{formatElapsed(props.elapsed!)}</text>
+        </Show>
         <Show when={props.children}>
           <text fg={color()}>{props.children}</text>
         </Show>
diff --git a/packages/opencode/src/cli/cmd/tui/routes/session/index.tsx b/packages/opencode/src/cli/cmd/tui/routes/session/index.tsx
index 131303269..db44ec40f 100644
--- a/packages/opencode/src/cli/cmd/tui/routes/session/index.tsx
+++ b/packages/opencode/src/cli/cmd/tui/routes/session/index.tsx
@@ -7,6 +7,7 @@ import {
   For,
   Match,
   on,
+  onCleanup,
   onMount,
   Show,
   Switch,
@@ -1842,6 +1843,7 @@ function InlineTool(props: {
   pending: string
   spinner?: boolean
   subagent?: boolean
+  elapsed?: number
   children: JSX.Element
   part: ToolPart
   onClick?: () => void
@@ -1967,7 +1969,7 @@ export function InlineToolRow(props: {
     >
       <Switch>
         <Match when={props.spinner}>
-          <Spinner color={props.color} children={props.children} />
+          <Spinner color={props.color} children={props.children} elapsed={props.elapsed} />
         </Match>
         <Match when={true}>
           <Show
@@ -2016,6 +2018,7 @@ function BlockTool(props: {
   onClick?: () => void
   part?: ToolPart
   spinner?: boolean
+  elapsed?: number
 }) {
   const { theme } = useTheme()
   const renderer = useRenderer()
@@ -2048,7 +2051,9 @@ function BlockTool(props: {
           </text>
         }
       >
-        <Spinner color={theme.textMuted}>{props.title.replace(/^# /, "")}</Spinner>
+        <Spinner color={theme.textMuted} elapsed={props.elapsed}>
+          {props.title.replace(/^# /, "")}
+        </Spinner>
       </Show>
       {props.children}
       <Show when={error()}>
@@ -2063,6 +2068,26 @@ function Shell(props: ToolProps<typeof ShellTool>) {
   const pathFormatter = usePathFormatter()
   const ctx = use()
   const isRunning = createMemo(() => props.part.state.status === "running")
+  const [startTime, setStartTime] = createSignal<number | null>(null)
+  const [elapsed, setElapsed] = createSignal(0)
+
+  createEffect(() => {
+    if (props.part.state.status === "running" && !startTime()) {
+      setStartTime(Date.now())
+    }
+  })
+
+  createEffect(() => {
+    if (!isRunning() || !startTime()) return
+    const update = () => {
+      const e = Date.now() - startTime()!
+      setElapsed(Math.floor(e / 1000))
+    }
+    update()
+    const id = setInterval(update, 1000)
+    onCleanup(() => clearInterval(id))
+  })
+
   const output = createMemo(() => stripAnsi(props.metadata.output?.trim() ?? ""))
   const [expanded, setExpanded] = createSignal(false)
   const maxLines = 10
@@ -2094,6 +2119,7 @@ function Shell(props: ToolProps<typeof ShellTool>) {
           title={title()}
           part={props.part}
           spinner={isRunning()}
+          elapsed={isRunning() ? elapsed() : undefined}
           onClick={collapsed().overflow ? () => setExpanded((prev) => !prev) : undefined}
         >
           <box gap={1}>
@@ -2108,7 +2134,14 @@ function Shell(props: ToolProps<typeof ShellTool>) {
         </BlockTool>
       </Match>
       <Match when={true}>
-        <InlineTool icon="$" pending="Writing command..." complete={props.input.command} part={props.part}>
+        <InlineTool
+          icon="$"
+          pending="Writing command..."
+          complete={props.input.command}
+          part={props.part}
+          spinner={isRunning()}
+          elapsed={isRunning() ? elapsed() : undefined}
+        >
           {props.input.command}
         </InlineTool>
       </Match>
